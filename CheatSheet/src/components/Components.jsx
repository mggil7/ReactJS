

// eslint-disable-next-line react/prop-types
const Components = ({name}) => {

    return (

    <div>
        <h2>Components</h2>
        <p>Hello, {name}!</p>
    </div>

    )
}

export default Components